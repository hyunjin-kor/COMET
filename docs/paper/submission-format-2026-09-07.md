# Submission format and source checks — 2026-09-07

Provisional preparation target: **ACS Engineering Au, Article**. The [revised target review](journal-targets-2026-09-07.md) reflects the user's JIF≥8 objective. The [official Engineering Au guidance](https://researcher-resources.acs.org/publish/author_guidelines?coden=aeacb3), updated2026-08-27, was retrieved HTTP200 on2026-09-07. Exact JIF year/JCR category/quartile, indexing and publication-cost coverage remain unconfirmed. This is not final journal selection.

The inspected Article guidance typically limits the abstract to300 words. No fixed Article main-text or figure limit was found; the2200-word rule is for Letters. The earlier7000-word-equivalent calculation belonged to ACS Sustainable Chemistry & Engineering and is no longer presented as the new target's rule. The manuscript retains unnumbered sections, embedded figures, separate SI and a TOC graphic. AI assistance is disclosed. These choices do not establish journal eligibility or editorial acceptance.

`manuscript_2026-09-07.md` retains six main figures from the original frozen primary run and two main tables. It adds a controlled follow-up using the identical reference/live snapshots, with one SI figure and seven SI tables in total. Counts and JSON-key-reference checks are regenerated in `submission_manuscript_checks_2026-09-07.json`; no invented target word-equivalent limit is applied. The abstract currently has182 lexical words. The TOC adds no new numerical scientific result.

The [linked TOC specification PDF](https://pubsapp.acs.org/paragonplus/submission/toc_abstract_graphics_guidelines.pdf) timed out in the browsing tool, then returned HTTP 200 through a direct public request. Its specifications were extracted locally: maximum 3.25 × 1.75 inches, sans-serif text at least 6 points, and color TIFF at 300 dpi. The generated companion TIFF is RGB, 975 × 525 pixels at 300 dpi with text at least 7 points; the SVG contains the same programmatic diagram. No source PDF is redistributed. The final DOCX/PDF export should use page numbers and hide source-traceability HTML comments. No submission or paid formatting service was used.

The new draft records AI assistance without inventing author names, affiliations, funding, conflicts, or human approval. Authors must supply those details and a cover letter before submission. This is a coherent scientific submission draft, not an assertion that author approval or editorial acceptance has occurred.

`submission-source-checks-2026-09-07.json` retains current HTTP/identifier outcomes. The Step Method, CatCost and BioSTEAM references matched Crossref; the existing project concept DOI was findable through DataCite and the repository returned HTTP 200. The Nuss–Eckelman Crossref request returned HTTP 429; it was not retried, and its unchanged bibliographic identity relies on the prior `../audit/paper-references-2026-09-06.json` verification. The concept DOI is not represented as a new submission-version deposit. Full publisher texts and third-party databases are not copied into this package.

The SI template-source guard accepts only exact raw, LF or CRLF representations of the manifest hash. This accommodates Git line-ending conversion while keeping every other byte strict. `submission-line-ending-check-2026-09-07.json` records direct assertions against the actual committed `aem_fuel_cell_ccm.json` blob: its LF hash differs from the Windows snapshot, but both LF and CRLF pass; changing the JSON `id` value or appending a space is rejected. No source file was rewritten. Frozen analysis outputs and retained source snapshots still require their exact byte hashes.

The controlled follow-up numerical JSON is checked against its original provenance hash and both selected manuscript price snapshots; the seed must agree. Its fixed files and the added DOI access record are marked byte-preserved for cross-platform checkout. New bibliographic references for Gkika/Kyzas and Ferdous et al. use the actual Crossref responses in `../sources/independent-evidence-access-2026-09-07.json`. These additional papers do not add matched independent manufacturing observations. The manuscript now distinguishes price/evidence channels, scale-class discontinuities, mixed catalog electrode boundaries and the unresolved legacy-data distribution review. Company interests and institutional/author approvals remain explicit confirmation items.
